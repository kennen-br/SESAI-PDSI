h1.page-title
  .breadcrumb-icon
  = breadcrumbs [{ name: 'PDSI', path: pdsis_path(section: 'estrutura') }, { name: section_name(@section), path: pdsis_path(section: @section_url) }]


= render "pdsis/sections/#{@section}"
