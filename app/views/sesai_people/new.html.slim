- content_for :breadcrumb do
  = breadcrumbs [{ name: 'Pessoas do DSEI', path: sesai_people_path }, { name: 'Cadastrar pessoa', path: nil }]

= form_for @person, url: { controller: 'sesai_people', action: 'create' } do |f|
  == render 'form', f: f
